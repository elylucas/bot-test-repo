name: Bot

on:
  issues:
    types: [opened]

jobs:
  bot:
    name: ${{ github.event_name }}/${{ github.event.action }}
    runs-on: ubuntu-latest
    steps:
      - uses: ionic-team/bot@160f210458dacf49008673b1e0cf9648ab350b4e
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}