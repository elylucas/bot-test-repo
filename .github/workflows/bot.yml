name: Bot

on:
  issues:
    types: [opened]

jobs:
  bot:
    name: ${{ github.event_name }}/${{ github.event.action }}
    runs-on: ubuntu-latest
    steps:
      - uses: ionic-team/bot@375fc8a3a3b6cde85d1a7eb7e11fbd62cbc4562c
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}